{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":dependencyDashboard"],
  "timezone": "Pacific/Auckland",
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "linuxserver.io images use date-style tags, not semver",
      "matchPackageNames": ["lscr.io/linuxserver/**"],
      "versioning": "loose"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "OpenZFS release pinned as ARG ZFS_VERSION in the Containerfile",
      "managerFilePatterns": ["/^Containerfile$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+) extractVersion=(?<extractVersion>\\S+)\\s+ARG ZFS_VERSION=(?<currentValue>\\S+)"
      ]
    }
  ],
  "automerge": true,
  "platformAutomerge": false
}
