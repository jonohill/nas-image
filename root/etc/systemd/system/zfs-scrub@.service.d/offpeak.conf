# Run inside the off-peak window only. The upstream unit pauses the scrub on
# stop and resumes it on start, so the window can close mid-scrub.
[Unit]
PartOf=offpeak.target

[Service]
ExecCondition=/usr/local/sbin/zfs-scrub-due.sh %i
